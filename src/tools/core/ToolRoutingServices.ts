/**
 * Compatibility shim — the tool routing service helpers now live in the
 * canonical home `src/tools/kernel/routing.ts`. Removed once all importers are
 * repointed to `#tools/kernel`.
 */

export * from '#tools/kernel/routing.js';
