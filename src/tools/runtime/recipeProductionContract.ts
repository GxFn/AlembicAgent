/** Shared prompt contract for every Agent surface that can call knowledge.submit. */
export const RECIPE_PRODUCTION_PROFILE_PROMPT = `## Recipe retrieval profile contract
- Every evidence-ready knowledge.submit must include params.retrievalProfile with: primaryLanguage; summary.primary; an evidence-grounded summary.technicalEnglish; structured concepts, scenarios, and exclusions; and provenance.sourceFieldRefs.
- Every concept/scenario/exclusion must carry provenanceRefs that point to an existing Recipe field (field:description, field:whenClause, field:doClause, field:dontClause, field:content.markdown, etc.) or to an exact bounded file:start-end citation already present in reasoning.sources. Documentation may ground retrieval-profile facts when that citation is project-local and exact.
- English profile text is retrieval expression only. It must restate cited project facts; never invent facts, frozen queries, expected IDs, global synonyms, provider/vector availability, ranking rules, or benchmark claims.
- Do not generate schemaVersion, sourceContentHash, generator, lifecycle, or publication state. The Agent adapter deterministically supplies profile identity/provenance, and Core alone evaluates readiness and publication eligibility.
- coreCode is optional. Include it only when it is verbatim contained in a cited, project-local, non-document, non-whole-file bounded range. Documentation is never eligible for coreCode. Never paste a whole file or guess/repair code from the first source. If code evidence is insufficient, omit coreCode while preserving any independently field/file-grounded retrieval profile.`;
