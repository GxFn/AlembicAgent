{
  "schemaVersion": 1,
  "description": "AlembicAgent Core consumer boundary. Agent code must consume stable @alembic/core facades instead of Core shared, service, or infrastructure internals. The lint default already denies every non-stable subpath that is not allowlisted here.",
  "scanRoots": ["src", "test", "scripts"],
  "notes": {
    "baselineScan": "Frozen 2026-06-11 from the real import scan of src/test/scripts: 248 files, 51 @alembic/core references, all stable-public; lint run: npm run lint:core-import-boundary --format json.",
    "stableFacadeEntries": "Entries for stable facades record the frozen baseline counts. The Core lint always allows stable facades and skips them in the limit check today; the counts become enforced ratchets automatically if Core ever demotes a facade from stable.",
    "zeroLimitEntries": "Entries with limit 0 are enforced deny rules: these Core internal subpaths must never be imported by Agent code. Any occurrence fails the lint with 'Reference limit exceeded: N found, baseline allows 0'.",
    "newImports": "A new non-stable @alembic/core subpath import fails by default. If a legitimate import must be added, record it here with a reason instead of loosening the gate to empty."
  },
  "referenceLimits": {
    "@alembic/core": 2,
    "@alembic/core/dimensions": 2,
    "@alembic/core/events": 6,
    "@alembic/core/evolution": 1,
    "@alembic/core/io": 5,
    "@alembic/core/knowledge": 2,
    "@alembic/core/logging": 29,
    "@alembic/core/project-intelligence": 1,
    "@alembic/core/search": 3,
    "@alembic/core/infrastructure/database": 0,
    "@alembic/core/infrastructure/database/drizzle": 0,
    "@alembic/core/infrastructure/vector": 0,
    "@alembic/core/repository": 0,
    "@alembic/core/repository/memory": 0,
    "@alembic/core/service": 0,
    "@alembic/core/shared": 0,
    "@alembic/core/workflows": 0
  }
}
