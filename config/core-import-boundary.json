{
  "schemaVersion": 1,
  "description": "AlembicAgent Core consumer boundary. Agent code must consume stable @alembic/core facades instead of Core shared, service, or infrastructure internals.",
  "scanRoots": ["src", "test", "scripts"],
  "referenceLimits": {}
}
